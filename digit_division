import  numpy as np

def digit_list(n):
    digits = [int(x) for x in str(n)]
    x = np.array(digits)
    x = np.unique(x)
    return x


m = int(input("Enter the number : "))
digit = digit_list(m)

print(digit)
